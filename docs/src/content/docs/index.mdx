---
title: data-path
description: Type-safe object path builder, accessor, and algebra for TypeScript.
template: splash
hero:
  title: Typed Object Paths for TypeScript
  tagline: Build paths from lambdas. Use them as strings, accessors, or composable algebra.
  actions:
    - text: Quick Start
      link: /getting-started/quick-start/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/sergeyshmakov/data-path
      icon: external
      variant: minimal
    - text: View on npm
      link: https://www.npmjs.com/package/data-path
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## Before / after

```ts
// Before — string literals, invisible to the compiler
register("users.0.profile.firstName");
table.getColumn("contact.email");
```

```ts
// After — typed, autocompleted, refactor-safe
register(path((u: FormData) => u.users[0].profile.firstName).$);
table.getColumn(emailPath.$);
```

## What it is

A zero-dependency TypeScript library that captures object property paths via proxy-based lambdas. Build a path once — use it as a string, read and write data through it, compose paths together, or match one against another.

- Typed root to leaf — renaming a property breaks the path at compile time
- Safe reads, immutable writes, structural clones
- Template paths for bulk operations across collections and trees
- Path algebra and relational queries

## Feature areas

<CardGrid>
  <Card title="Data access" icon="document">
    `get`, `set`, `update`, and `fn` — read and write through a typed path.

    [Data Access guide](/guides/data-access/)
  </Card>
  <Card title="Templates" icon="list-format">
    `each` and `deep` for bulk reads and writes across arrays and recursive trees.

    [Templates guide](/guides/templates/)
  </Card>
  <Card title="Path algebra" icon="puzzle">
    `merge`, `subtract`, `slice`, `to` — compose and decompose paths.

    [Path Algebra guide](/guides/path-algebra/)
  </Card>
  <Card title="Integrations" icon="rocket">
    Works with React Hook Form, Zustand, TanStack, Zod, and more.

    [Integrations](/integrations/react-hook-form/)
  </Card>
</CardGrid>

## Next steps

<CardGrid>
  <Card title="Install" icon="download">
    Add the package and verify the setup.

    [Installation](/getting-started/installation/)
  </Card>
  <Card title="Quick start" icon="right-arrow">
    A working example in under five minutes.

    [Quick Start](/getting-started/quick-start/)
  </Card>
  <Card title="API cheatsheet" icon="open-book">
    Every method at a glance.

    [API Cheatsheet](/reference/api-cheatsheet/)
  </Card>
  <Card title="Types reference" icon="information">
    `Path`, `TemplatePath`, `ResolvedType`, and more.

    [Types](/reference/types/)
  </Card>
</CardGrid>
